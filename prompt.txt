## Context & Status
- **Status Perbaikan Sebelumnya:** - Penyesuaian skala/dimensi bitmap (`bitmapWidth`/`bitmapHeight`) dan logika *overlay* teks di `ReaderActivity.kt` sudah berhasil diperbaiki.
  - Eksekusi OCR otomatis via `triggerOcrForCurrentPage()` di `ReaderViewModel.kt` sudah diimplementasikan.
- **Acuan Referensi:** Dokumentasi standar integrasi ML Kit dan Text Recognition telah disimpan di file `DOCS_MLKIT.md` pada root/home proyek.

## Issue Saat Ini
1. **Pemeriksaan Integrasi ML Kit:** Perlu dipastikan pipeline ekstraksi teks dari *bitmap* hasil tangkapan layar sudah menggunakan acuan *best practice* dari ML Kit yang tertera pada `DOCS_MLKIT.md`.
2. **Indikator Sistem Android Hilang:** Perekaman layar via `MediaProjection` dan `VirtualDisplay` perlu diverifikasi kembali agar sistem Android menampilkan indikator *screen capture* (perekaman/cast) aktif di *status bar*, bukan bergantung pada metode *fallback view capture*.

## Tugas & Spesifikasi Perbaikan

Tolong selesaikan perbaikan ini dengan mengikuti petunjuk berikut:

1. **Baca File Referensi `DOCS_MLKIT.md`:**
   - Wajib baca file `DOCS_MLKIT.md` yang berada di root/home proyek sebelum melakukan perubahan kode.
   - Pelajari struktur pembuatan `TextRecognitionHelper`, penggunaan `InputImage`, serta penanganan *recognizer* (termasuk opsi dukungan multi-bahasa seperti Jepang/Mandarin/Korea jika diperlukan).

2. **Verifikasi Pipeline OCR ML Kit:**
   - Pastikan *bitmap* yang ditangkap oleh `ImageReader` diubah menjadi `InputImage` dan diproses menggunakan `TextRecognition.getClient()` sesuai acuan di `DOCS_MLKIT.md`.
   - Pastikan *listener* sukses/gagal menangani hasil rekognisi teks dan mengumpankannya kembali ke `ReaderViewModel`.

3. **Perbaiki `MediaProjection` & `VirtualDisplay`:**
   - Pastikan `VirtualDisplay` terhubung secara riil dengan `Surface` dari `ImageReader`, sehingga OS Android secara resmi mengaktifkan indikator *screen recording* di *status bar*.
   - Hindari penggunaan *fallback view capture* secara penuh jika izin `MediaProjection` sudah diberikan oleh pengguna (`RESULT_OK`).

4. **Testing & Clean Code (WAJIB):**
   - Jalankan `./gradlew spotlessApply` untuk merapikan format kode.
   - Jalankan `./gradlew testDebugUnitTest` dan pastikan **BUILD SUCCESSFUL**.
